version: '3.8'

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./bind/conf.d/:/etc/nginx/conf.d/:ro
      - ./bind/root/:/var/www/:ro
      - ./bind/cert/:/etc/nginx/ssl/:ro
      - ./bind/nginx.conf:/etc/nginx/nginx.conf:ro

  certbot:
    image: certbot/certbot:latest
    volumes:
      - ./bind/root/certbot/:/var/www/certbot/:rw
      - ./bind/cert/:/etc/letsencrypt/:rw
