myhosts:
  hosts:
    rpi:
      ansible_host: aloeil.ddns.net
      ansible_connection: ssh
      ansible_ssh_user: dominique
      ansible_ssh_pass: "{{ vault.host.rpi.ssh_pass }}"
      ansible_become: true
